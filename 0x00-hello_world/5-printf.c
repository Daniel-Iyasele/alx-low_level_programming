#include <stdio.h>
/*this program uses printf() function */
/**
 * main -entrypoint
 *
 * Return: always 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
